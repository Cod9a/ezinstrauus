<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="keywords" content="EZIN & STRAUUS CP, Cabinet, EZIN, STRAUUS, Experts comptables">
    <meta name="description" content="Cabinet EZIN & STRAUUS">
    <meta name="author" content="Cabinet EZIN & STRAUUS">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>@yield('title') &bull; Cabinet EZIN & STRAUUS CP</title>

    <!-- favicon icon -->
    <link rel="shortcut icon" href="{{ asset((!\App::environment('local') ? 'public/' : '') . 'storage/images/ES.jpg') }}">

    <!-- bootstrap -->
    <link rel="stylesheet" type="text/css" href="{{ asset((!\App::environment('local') ? 'public/' : '') . 'storage/css/bootstrap.min.css') }}">

    <!-- animate -->
    <link rel="stylesheet" type="text/css" href="{{ asset((!\App::environment('local') ? 'public/' : '') . 'storage/css/animate.css') }}">

    <!-- flaticon -->
    <link rel="stylesheet" type="text/css" href="{{ asset((!\App::environment('local') ? 'public/' : '') . 'storage/css/flaticon.css') }}">

    <!-- fontawesome -->
    <link rel="stylesheet" type="text/css" href="{{ asset((!\App::environment('local') ? 'public/' : '') . 'storage/css/font-awesome.css') }}">

    <!-- themify -->
    <link rel="stylesheet" type="text/css" href="{{ asset((!\App::environment('local') ? 'public/' : '') . 'storage/css/themify-icons.css') }}">

    <!-- slick -->
    <link rel="stylesheet" type="text/css" href="{{ asset((!\App::environment('local') ? 'public/' : '') . 'storage/css/slick.css') }}">

    <!-- prettyphoto -->
    <link rel="stylesheet" type="text/css" href="{{ asset((!\App::environment('local') ? 'public/' : '') . 'storage/css/prettyPhoto.css') }}">

    <!-- shortcodes -->
    <link rel="stylesheet" type="text/css" href="{{ asset((!\App::environment('local') ? 'public/' : '') . 'storage/css/shortcode.css') }}">

    <!-- main -->
    <link rel="stylesheet" type="text/css" href="{{ asset((!\App::environment('local') ? 'public/' : '') . 'storage/css/main.css') }}">

    <!-- megamenu -->
    <link rel="stylesheet" type="text/css" href="{{ asset((!\App::environment('local') ? 'public/' : '') . 'storage/css/megamenu.css') }}">

    <!-- responsive -->
    <link rel="stylesheet" type="text/css" href="{{ asset((!\App::environment('local') ? 'public/' : '') . 'storage/css/responsive.css') }}">

    <!-- custom -->
    <link rel="stylesheet" type="text/css" href="{{ asset((!\App::environment('local') ? 'public/' : '') . 'storage/css/custom.css') }}">
</head>